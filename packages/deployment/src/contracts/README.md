These files are already compiled and saved in `packages/deployment/artifacts-saved`. The source code is saved here, in
the `src/` folder, so, it can be inspected in the future while not being compiled again.
